# Brick Borrow — Missing Piece Data

Static JSON catalogue of parts and minifigures for Brick Borrow's rental fleet, derived from Rebrickable data. Powers the missing-piece Jotform widget.

- `index.json` — list of all set numbers/names in the fleet
- `parts/<set_number>.json` — non-spare parts + minifigures for that set, with images
